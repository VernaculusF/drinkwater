# 📱 Публикация в Google Play Store

## Подготовка к публикации

### 1. Создание ключа для подписи APK

```bash
keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```

Сохраните пароль и алиас в безопасном месте!

### 2. Настройка подписи

Создайте файл `android/key.properties`:

```properties
storePassword=ваш_пароль
keyPassword=ваш_пароль
keyAlias=upload
storeFile=/путь/к/upload-keystore.jks
```

### 3. Обновите android/app/build.gradle

Добавьте перед блоком `android`:

```gradle
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}
```

Обновите блок `buildTypes`:

```gradle
buildTypes {
    release {
        signingConfig signingConfigs.release
    }
}

signingConfigs {
    release {
        keyAlias keystoreProperties['keyAlias']
        keyPassword keystoreProperties['keyPassword']
        storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
        storePassword keystoreProperties['storePassword']
    }
}
```

### 4. Сборка релизного APK

```bash
flutter build apk --release
```

Или для App Bundle (рекомендуется):

```bash
flutter build appbundle --release
```

## Материалы для Google Play

### Скриншоты (обязательно)
- Минимум 2 скриншота
- Разрешение: 1920x1080 или другое стандартное
- Показать главный экран и экран настроек

### Feature Graphic (обязательно)
- Размер: 1024x500 px
- Баннер для магазина

### Иконка приложения (обязательно)
- Размер: 512x512 px
- Формат: PNG с прозрачностью

### Описание приложения

**Короткое описание (80 символов):**
```
Минималистичный трекер воды с юмористическими напоминаниями. Без регистрации!
```

**Полное описание:**
```
💧 ПЕЙ ВОДУ — это трекер воды, который не учит жить, а мотивирует с юмором!

✨ ОСОБЕННОСТИ:
• Без регистрации и аккаунтов — всё локально
• Без графиков и сложной аналитики — только привычка
• Юмористические напоминания вместо скучных уведомлений
• 3 уровня токсичности: Лайт, Средний, Душнила
• Автоматический расчёт нормы воды по весу
• Настраиваемые тихие часы
• Интервалы напоминаний: 1, 2, 3 часа
• Поддержка тёмной темы

🎯 КАК ЭТО РАБОТАЕТ:
1. Выпил стакан воды → нажал кнопку
2. Приложение отслеживает прогресс
3. Получай стёбные напоминания, когда пора пить

🔒 КОНФИДЕНЦИАЛЬНОСТЬ:
• Нет сбора данных
• Нет интернета
• Нет аналитики
• Всё хранится на вашем устройстве

📱 ТРЕБОВАНИЯ:
• Android 7.0 или выше
• Разрешение на уведомления (для напоминаний)

Пей воду регулярно — твоё тело скажет спасибо! 💪
```

### Категория
Здоровье и фитнес

### Рейтинг контента
Для всех возрастов (без мата и оскорблений)

### Política de privacidade (Privacy Policy)

Создайте простую страницу:

```
Политика конфиденциальности

Приложение "ПЕЙ ВОДУ" не собирает никаких личных данных пользователей.

Все данные хранятся локально на устройстве пользователя.

Приложение не использует интернет-соединение для передачи данных.

Мы не собираем, не храним и не передаём третьим лицам никакую информацию.

Контакты: [ваш email]
```

### Теги (keywords)
вода, трекер воды, напоминания, здоровье, гидратация, юмор

## Чек-лист перед публикацией

- [ ] Подписан релизный APK/AAB
- [ ] Протестировано на реальном устройстве
- [ ] Все уведомления работают корректно
- [ ] Добавлена иконка приложения
- [ ] Подготовлены скриншоты
- [ ] Написано описание
- [ ] Создана страница Privacy Policy
- [ ] Проверена работа на Android 7.0+
- [ ] Убраны все TODO и console.log в production коде
- [ ] Проверена работа без интернета

## Рекомендации

### Версионирование
```yaml
version: 1.0.0+1
# Формат: major.minor.patch+build
```

Для обновлений:
```yaml
version: 1.0.1+2
version: 1.1.0+3
```

### Тестирование перед релизом
1. Установите режим `isInDebugMode: false` в WorkManager
2. Протестируйте минимум на 3 разных устройствах
3. Проверьте на разных версиях Android (7.0, 10, 13+)

### Первый релиз
- Публикуйте как "Закрытое тестирование (Internal Testing)"
- Соберите отзывы от 10-20 пользователей
- Исправьте критические баги
- Переведите в "Открытое тестирование" или сразу в "Production"

## Монетизация (для будущих версий)

### Google AdMob
1. Создайте аккаунт AdMob
2. Добавьте рекламный блок
3. Интегрируйте google_mobile_ads
4. Тестируйте с тестовыми ID

### In-App Purchases
1. Настройте продукты в Google Play Console
2. Создайте товар "Убрать рекламу" за 199₽
3. Интегрируйте in_app_purchase
4. Протестируйте с тестовыми аккаунтами

## Поддержка и обновления

После публикации:
1. Мониторьте отзывы пользователей
2. Отвечайте на вопросы
3. Собирайте feedback для новых функций
4. Выпускайте обновления раз в 1-2 месяца

---

**Удачи с публикацией! 🚀**
